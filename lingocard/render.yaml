version: 1
services:
  - type: web
    name: lingo-card-1 # This is the service name
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python manage.py runserver 0.0.0.0:8000
    systemPackages:
      - libxml2-dev
      - libxslt-dev